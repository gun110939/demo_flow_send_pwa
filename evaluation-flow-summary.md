# เอกสารสรุป: Flow การประเมินตามลำดับสายบังคับบัญชา
**Document Version:** 1.0  
**วันที่:** 23 พฤศจิกายน 2025  
**สำหรับ:** ทีม Development

---

## 📋 สารบัญ
1. [ภาพรวมระบบ](#1-ภาพรวมระบบ)
2. [โครงสร้างข้อมูล](#2-โครงสร้างข้อมูล)
3. [Flow การประเมิน](#3-flow-การประเมิน)
4. [Logic และเงื่อนไข](#4-logic-และเงื่อนไข)
5. [Use Cases และตัวอย่าง](#5-use-cases-และตัวอย่าง)
6. [Edge Cases](#6-edge-cases)
7. [สถิติและข้อมูลสำคัญ](#7-สถิติและข้อมูลสำคัญ)
8. [Data Structure แนะนำ](#8-data-structure-แนะนำ)
9. [Checklist สำหรับ Dev](#9-checklist-สำหรับ-dev)

---

## 1. ภาพรวมระบบ

### 1.1 วัตถุประสงค์
ระบบประเมินผลงานพนักงานตามลำดับสายบังคับบัญชา โดยผลงานจะถูกส่งต่อเพื่อประเมินตามลำดับชั้น จนกระทั่งถึงคณะกรรมการกลั่นกรองและคณะกรรมการขั้นสุดท้าย

### 1.2 ผู้ใช้งานในระบบ
- **พนักงานทั่วไป:** ส่งผลงานและประเมินผลงานในหน่วยงานของตน (8,915 คน)
- **คณะกรรมการกลั่นกรอง (ก่อนลำดับสุดท้าย):** ประเมินผลงานใน PARENTORG ของตนเอง
- **คณะกรรมการ (ลำดับสุดท้าย):** ประเมินผลงานทั้งหมดที่ผ่านการกลั่นกรอง

### 1.3 ระดับพนักงาน (PERSK)
- ระดับ 1-7: พนักงานปฏิบัติการ (6,965 คน - 78%)
- ระดับ 8-9: หัวหน้างาน/ผู้จัดการ (1,748 คน - 20%)
- ระดับ 10-12: ผู้บริหาร (68 คน - 0.76%)
- ระดับ 21: ผู้ว่าการ (1 คน)
- ระดับ 31-32: อื่นๆ (133 คน - 1.49%)

---

## 2. โครงสร้างข้อมูล

### 2.1 ฟิลด์สำคัญในระบบ

| ฟิลด์ | ประเภท | คำอธิบาย | ตัวอย่าง |
|-------|--------|----------|----------|
| **PERNR** | Integer | รหัสพนักงาน (Primary Key) | 5514 |
| **ENAME** | String | ชื่อ-นามสกุล | นาย อำพล สุดนิคม |
| **STELL** | String | ตำแหน่งงาน | นักบริหาร ระดับ 9 |
| **PERSK** | Integer | ระดับพนักงาน (1-12, 21, 31-32) | 9 |
| **ORGEH** | String | หน่วยงาน (608 หน่วยงาน) | งานจัดเก็บรายได้ 1 |
| **PARENTORG** | String | กอง/เขต (166 กอง/เขต) | การประปาส่วนภูมิภาคเขต 8 |
| **MGRPERNR** | Integer | รหัสผู้บังคับบัญชาโดยตรง | 7135 |

### 2.2 จำนวนข้อมูล
- **พนักงานทั้งหมด:** 8,915 คน
- **PARENTORG (กอง/เขต):** 166 แห่ง
- **ORGEH (หน่วยงาน):** 608 หน่วยงาน
- **ความยาว Chain of Command:** 
  - ค่าเฉลี่ย: 5.72 ขั้น
  - สั้นที่สุด: 1 ขั้น
  - ยาวที่สุด: 7 ขั้น

---

## 3. Flow การประเมิน

### 3.1 Flow หลัก

```
┌─────────────────────────────────────────────────────────────────┐
│ พนักงานส่งผลงาน                                                  │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ ตรวจสอบเงื่อนไข:                                                │
│ • มี PARENTORG หรือไม่?                                          │
│ • มี MGRPERNR หรือไม่?                                           │
└────────────────────────┬────────────────────────────────────────┘
                         │
                ┌────────┴────────┐
                │                 │
        ไม่มี PARENTORG     มี PARENTORG
                │                 │
                │                 ▼
                │    ┌─────────────────────────────────────────────┐
                │    │ ส่งไปประเมินตามสายบังคับบัญชา (MGRPERNR)    │
                │    │ ระดับที่ 1 → 2 → 3 → ... → สูงสุดถึงระดับ 10│
                │    └────────────────┬────────────────────────────┘
                │                     │
                │            ┌────────┴────────┐
                │            │                 │
                │  ระดับถัดไปคือ 10/11    ระดับอื่นๆ
                │            │                 │
                │            │                 ▼
                │            │        ส่งต่อเรื่อยๆ
                │            │                 │
                │            ▼                 │
                │    ┌───────────────┐         │
                │    │ เงื่อนไขพิเศษ │         │
                │    │ มีการประเมิน  │         │
                │    │ แล้ว >= 2 ครั้ง?│        │
                │    └───────┬───────┘         │
                │            │                 │
                │       ┌────┴────┐            │
                │       │         │            │
                │      ใช่       ไม่          │
                │       │         │            │
                │       │         ▼            │
                │       │  บังคับส่งไป         │
                │       │  ประเมินที่ 10/11    │
                │       │         │            │
                ▼       ▼         ▼            │
┌─────────────────────────────────────────────┴────────────────────┐
│ ส่งเข้า "ก่อนลำดับสุดท้าย" (คณะกรรมการกลั่นกรอง)                │
│ • แต่ละคนเห็นเฉพาะผลงานใน PARENTORG ของตัวเอง                    │
└────────────────────────┬─────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│ ส่งเข้า "ลำดับสุดท้าย" (คณะกรรมการ)                             │
│ • เห็นผลงานทั้งหมดที่ส่งมาจาก "ก่อนลำดับสุดท้าย"                │
└─────────────────────────────────────────────────────────────────┘
```

### 3.2 การมองเห็นผลงาน (Visibility Rules)

| ผู้ใช้งาน | ขอบเขตการมองเห็น | เงื่อนไข |
|-----------|------------------|----------|
| **พนักงานทั่วไป** | เห็นเฉพาะผลงานใน PARENTORG เดียวกันเท่านั้น | ต้อง PARENTORG ตรงกัน |
| **คณะกรรมการกลั่นกรอง** | เห็นเฉพาะผลงานใน PARENTORG ของตัวเอง | ต้อง PARENTORG ตรงกัน |
| **คณะกรรมการ** | เห็นผลงานทั้งหมดที่ผ่านการกลั่นกรอง | ไม่จำกัด PARENTORG |

---

## 4. Logic และเงื่อนไข

### 4.1 เงื่อนไขการส่งต่อตามสายบังคับบัญชา

#### 🔴 เงื่อนไขหลัก
```javascript
// Pseudo Code
function sendToNextEvaluator(workResult) {
  // 1. ตรวจสอบ PARENTORG
  if (!workResult.employee.PARENTORG || workResult.employee.PARENTORG === null) {
    return sendToPreFinalCommittee(workResult);
  }
  
  // 2. ตรวจสอบ Chain of Command
  let currentManager = workResult.employee.MGRPERNR;
  let evaluationCount = workResult.evaluationHistory.length;
  
  while (currentManager !== null) {
    let manager = getEmployeeByPERNR(currentManager);
    
    // 3. ตรวจสอบระดับ (PERSK)
    if (manager.PERSK >= 10) {
      // เงื่อนไขพิเศษ: มีการประเมินแล้ว >= 2 ครั้ง + ลำดับถัดไปคือ 10/11
      if (evaluationCount >= 2) {
        return sendToPreFinalCommittee(workResult);
      } else {
        // บังคับส่งไปประเมินที่ระดับ 10/11 ก่อน
        return sendToEvaluator(manager, workResult);
      }
    }
    
    // 4. ส่งต่อไปประเมินที่ manager คนถัดไป
    sendToEvaluator(manager, workResult);
    evaluationCount++;
    currentManager = manager.MGRPERNR;
  }
  
  // 5. ถ้าไม่มี manager แล้ว ส่งเข้าคณะกรรมการกลั่นกรอง
  return sendToPreFinalCommittee(workResult);
}
```

### 4.2 กฎการส่งต่อแบบละเอียด

#### Rule 1: ส่งต่อปกติ
- **เงื่อนไข:** PERSK ของ manager ถัดไป < 10
- **การทำงาน:** ส่งต่อไปประเมินเรื่อยๆ ตาม MGRPERNR
- **ตัวอย่าง:** ระดับ 6 → 7 → 8 → 9 → ...

#### Rule 2: หยุดที่ระดับ 10 (มีการประเมินแล้ว >= 2 ครั้ง)
- **เงื่อนไข:** 
  - `evaluationCount >= 2`
  - `nextManager.PERSK >= 10`
- **การทำงาน:** ข้ามการประเมินที่ระดับ 10/11 ไปเข้า "ก่อนลำดับสุดท้าย" เลย
- **ตัวอย่าง:** 
  ```
  ระดับ 6 → 7 (ประเมิน 1) → 8 (ประเมิน 2) → [ถัดไปคือ 10] 
  → ข้ามไป "ก่อนลำดับสุดท้าย"
  ```

#### Rule 3: บังคับประเมินที่ระดับ 10/11 (ยังไม่มีการประเมิน)
- **เงื่อนไข:** 
  - `evaluationCount < 2`
  - `nextManager.PERSK >= 10`
- **การทำงาน:** บังคับส่งไปประเมินที่ระดับ 10/11 ก่อน แล้วค่อยเข้า "ก่อนลำดับสุดท้าย"
- **ตัวอย่าง:** 
  ```
  ระดับ 9 (ยังไม่มีการประเมิน) → [ถัดไปคือ 11] 
  → บังคับส่งไปประเมินที่ 11 ก่อน → แล้วเข้า "ก่อนลำดับสุดท้าย"
  ```

#### Rule 4: ไม่มี PARENTORG
- **เงื่อนไข:** `employee.PARENTORG === null || employee.PARENTORG === ""`
- **การทำงาน:** ส่งเข้า "ก่อนลำดับสุดท้าย" ทันที
- **กรณีใช้งาน:** พนักงานที่ไม่สังกัดกอง/เขตใดๆ (พบ 5 คน)

#### Rule 5: ไม่มี MGRPERNR
- **เงื่อนไข:** `employee.MGRPERNR === null`
- **การทำงาน:** ส่งเข้า "ก่อนลำดับสุดท้าย" ทันที
- **กรณีใช้งาน:** ระดับสูงสุดในสายงาน (พบ 4 คน)

### 4.3 สถานะการประเมิน (Evaluation Status)

```typescript
enum EvaluationStatus {
  PENDING = "PENDING",           // รอการประเมิน
  IN_PROGRESS = "IN_PROGRESS",   // กำลังประเมิน
  APPROVED = "APPROVED",         // อนุมัติ
  REJECTED = "REJECTED",         // ไม่อนุมัติ
  SENT_TO_COMMITTEE = "SENT_TO_COMMITTEE"  // ส่งเข้าคณะกรรมการ
}

enum CommitteeStage {
  PRE_FINAL = "PRE_FINAL",      // ก่อนลำดับสุดท้าย (กลั่นกรอง)
  FINAL = "FINAL"               // ลำดับสุดท้าย
}
```

---

## 5. Use Cases และตัวอย่าง

### 5.1 Case 1: Chain สั้น (ไม่มีระดับ 10/11)

**สถานการณ์:** พนักงานระดับ 8 ส่งผลงาน และในสายบังคับบัญชาไม่มีระดับ 10/11

**ตัวอย่างจริง:**
```
ผู้ส่งผลงาน: นาง ปทิตตา พรมนนท์ (PERSK: 8) - กองบริหารทั่วไป

Flow การประเมิน:
  1. นาง ปทิตตา พรมนนท์ (นักบัญชี ระดับ 8) - PERSK: 8
  2. นาย อิทธิพร ทองงอก (หัวหน้างานพัสดุและบัญชีทรัพย์สิน 8) - PERSK: 8
     └─> ประเมิน ✓ (ครั้งที่ 1)
  3. นาง ภานุมาศ วงศ์พรหม (ผู้อำนวยการกองบริหารทั่วไป) - PERSK: 9
     └─> ประเมิน ✓ (ครั้งที่ 2)
  4. [ไม่มี MGRPERNR]
     └─> ส่งเข้า "ก่อนลำดับสุดท้าย" ✓

ผลลัพธ์: ประเมินได้ 2 ครั้ง → เข้าคณะกรรมการกลั่นกรอง
```

### 5.2 Case 2: Chain ยาว (มีระดับ 10 ใน Chain)

**สถานการณ์:** พนักงานระดับ 7 ส่งผลงาน และมีระดับ 10 ใน Chain + มีการประเมินแล้ว >= 2 ครั้ง

**ตัวอย่างจริง:**
```
ผู้ส่งผลงาน: นาง น้ำเพชร อยู่โภชนา (PERSK: 7) - กองบัญชี

Flow การประเมิน:
  1. นาง น้ำเพชร อยู่โภชนา (พนักงานการเงินและบัญชี ระดับ 7) - PERSK: 7
  2. นาง ทัศมลพรรณ เจริญวานิช (หัวหน้างานประมวลบัญชี 8) - PERSK: 8
     └─> ประเมิน ✓ (ครั้งที่ 1)
  3. น.ส. ณภัทร สฤษฎ์ศรี (ผู้อำนวยการกองบัญชี) - PERSK: 9
     └─> ประเมิน ✓ (ครั้งที่ 2)
  4. นาง ชนันพร แหลมทอง (ผู้อำนวยการฝ่ายการเงินและบัญชี) - PERSK: 10 ⚠️
     └─> มีการประเมินแล้ว 2 ครั้ง + ระดับ 10
     └─> ข้ามการประเมิน → ส่งเข้า "ก่อนลำดับสุดท้าย" ✓

ผลลัพธ์: ประเมินได้ 2 ครั้ง → ข้ามระดับ 10 → เข้าคณะกรรมการกลั่นกรอง
```

### 5.3 Case 3: บังคับประเมินที่ระดับ 10/11

**สถานการณ์:** พนักงานระดับ 9 ส่งผลงาน และยังไม่มีการประเมิน แต่ผู้บังคับบัญชาคือระดับ 11

**ตัวอย่างจริง:**
```
ผู้ส่งผลงาน: นาย ศักรินทร์ ถนอมพร (PERSK: 9) - การประปาส่วนภูมิภาคเขต 5

Flow การประเมิน:
  สถานะ: ยังไม่มีการประเมิน (0 ครั้ง)
  ลำดับถัดไป: นาย ประทีป กุศลวัฒนะ (ผู้อำนวยการเขต 5) - PERSK: 11
  
  เงื่อนไข:
  • evaluationCount = 0 < 2
  • nextManager.PERSK = 11 >= 10
  
  การทำงาน:
  └─> บังคับส่งไปประเมินที่ระดับ 11 ก่อน ✓
  └─> หลังจากประเมินแล้ว → ส่งเข้า "ก่อนลำดับสุดท้าย" ✓

ผลลัพธ์: บังคับประเมินที่ระดับ 11 → เข้าคณะกรรมการกลั่นกรอง
```

### 5.4 Case 4: ไม่มี PARENTORG

**สถานการณ์:** พนักงานที่ไม่สังกัดกอง/เขตใดๆ

**ตัวอย่างจริง:**
```
ผู้ส่งผลงาน: นาย คงศักดิ์ ยิ้มกุ่ย (PERSK: 8) - PARENTORG: NULL

Flow การประเมิน:
  1. ตรวจสอบ PARENTORG → NULL ⚠️
  2. ส่งเข้า "ก่อนลำดับสุดท้าย" ทันที ✓
     (ไม่มีการประเมินตามสายบังคับบัญชา)

ผลลัพธ์: ข้ามการประเมินปกติ → เข้าคณะกรรมการกลั่นกรองเลย
```

### 5.5 Case 5: Visibility ตาม PARENTORG

**สถานการณ์:** ผู้ประเมินเห็นผลงานเฉพาะในกอง/เขตของตนเองเท่านั้น

**ตัวอย่าง:**
```
นาย A (PARENTORG: "การประปาส่วนภูมิภาค สาขาส") ส่งผลงาน
นาย B (PARENTORG: "การประปาส่วนภูมิภาค สาขาส") → เห็นผลงานของ นาย A ✓
นาย C (PARENTORG: "การประปาส่วนภูมิภาค สาขาบ") → ไม่เห็นผลงานของ นาย A ✗

คณะกรรมการกลั่นกรอง (PARENTORG: "การประปาส่วนภูมิภาค สาขาส")
  → เห็นเฉพาะผลงานจาก "สาขาส" ที่ส่งเข้ามา ✓

คณะกรรมการ (ลำดับสุดท้าย)
  → เห็นผลงานทั้งหมดที่ผ่านการกลั่นกรอง ✓
```

---

## 6. Edge Cases

### 6.1 กรณีพิเศษที่ต้องจัดการ

#### Edge Case 1: MGRPERNR ไม่มีในระบบ
```javascript
// สถานการณ์: MGRPERNR ชี้ไปที่พนักงานที่ไม่มีในฐานข้อมูล
if (!getEmployeeByPERNR(MGRPERNR)) {
  // แนะนำ: ส่งเข้า "ก่อนลำดับสุดท้าย" เลย
  sendToPreFinalCommittee(workResult);
}
```

#### Edge Case 2: Circular Reference (วนลูป)
```javascript
// สถานการณ์: A → B → C → A (วนกลับมา)
// วิธีแก้: ใช้ Set เก็บ PERNR ที่ผ่านแล้ว
let visited = new Set();
while (currentManager !== null) {
  if (visited.has(currentManager)) {
    // พบการวนลูป → หยุดและส่งเข้าคณะกรรมการ
    console.error("Circular reference detected!");
    sendToPreFinalCommittee(workResult);
    break;
  }
  visited.add(currentManager);
  // ... ดำเนินการต่อ
}
```

#### Edge Case 3: ระดับพิเศษ (PERSK = 21, 31, 32)
```javascript
// สถานการณ์: พนักงานระดับพิเศษ (21=ผู้ว่าการ, 31-32=อื่นๆ)
if (manager.PERSK === 21 || manager.PERSK >= 31) {
  // แนะนำ: ถือว่าถึงระดับสูงสุดแล้ว
  sendToPreFinalCommittee(workResult);
}
```

#### Edge Case 4: ไม่มีผู้ประเมินเลย
```javascript
// สถานการณ์: MGRPERNR = NULL ตั้งแต่ต้น
if (!employee.MGRPERNR) {
  // ส่งเข้า "ก่อนลำดับสุดท้าย" เลย
  sendToPreFinalCommittee(workResult);
}
```

#### Edge Case 5: ผลงานถูก Reject
```javascript
// สถานการณ์: ผู้ประเมินไม่อนุมัติผลงาน
if (evaluation.status === "REJECTED") {
  // แนะนำ: ส่งกลับให้พนักงานแก้ไข หรือสิ้นสุด Flow
  // ไม่ส่งต่อไปประเมินขั้นถัดไป
  notifyEmployee(workResult, "กรุณาแก้ไขผลงานตามข้อเสนอแนะ");
}
```

### 6.2 พนักงานที่ไม่มี PARENTORG (5 คน)

| PERNR | ชื่อ | ตำแหน่ง | PERSK |
|-------|------|---------|-------|
| - | น.ส. พัชรินทร์ ทองผดุงโรจน์ | นักบริหาร ระดับ 9 | 9 |
| - | นาย คงศักดิ์ ยิ้มกุ่ย | นักบริหารงานทั่วไป ระดับ 8 | 8 |
| - | นาย อาทิตย์ สายปิง | วิศวกร ระดับ 8 | 8 |
| - | นาย แสงสว่าง ประจงมูล | นักบริหารงานทั่วไป ระดับ 6 | 6 |
| - | นาย จักรพงศ์ คำจันทร์ | ผู้ว่าการ | 21 |

**การจัดการ:** ผลงานของบุคคลเหล่านี้ส่งเข้า "ก่อนลำดับสุดท้าย" ทันที

### 6.3 พนักงานที่ไม่มี MGRPERNR (4 คน)

| PERNR | ชื่อ | ตำแหน่ง | PERSK | PARENTORG |
|-------|------|---------|-------|-----------|
| - | นาง ภานุมาศ วงศ์พรหม | ผู้อำนวยการกองบริหารทั่วไป | 9 | ผช.ผอ.การประปาส่วนภูมิภาค |
| - | นาย ทรงพล พนาลิกุล | ผช.ผอ.การประปาส่วนภูมิภาคเขต (วิชาการ) | 10 | การประปาส่วนภูมิภาคเขต 8 |
| - | น.ส. ศุภมาศ ไชยแก้ว | นักบริหารงานทั่วไป ระดับ 7 | 7 | การประปาส่วนภูมิภาค |
| - | น.ส. อาริยา อังคณาแสงมณี | นักบริหารงานทั่วไป ระดับ 7 | 7 | การประปาส่วนภูมิภาค |

**การจัดการ:** ถือว่าถึงระดับสูงสุดในสายงานแล้ว ส่งเข้า "ก่อนลำดับสุดท้าย"

---

## 7. สถิติและข้อมูลสำคัญ

### 7.1 จำนวนพนักงานตามระดับ

| ระดับ (PERSK) | จำนวน (คน) | สัดส่วน |
|---------------|------------|---------|
| 1 | 3 | 0.03% |
| 2 | 2 | 0.02% |
| 3 | 229 | 2.57% |
| 4 | 897 | 10.06% |
| 5 | 770 | 8.64% |
| 6 | 1,804 | 20.24% |
| 7 | 3,260 | 36.57% |
| 8 | 1,406 | 15.77% |
| 9 | 342 | 3.84% |
| **10** | **46** | **0.52%** ⚠️ |
| **11** | **16** | **0.18%** ⚠️ |
| 12 | 6 | 0.07% |
| 21 | 1 | 0.01% |
| 31 | 132 | 1.48% |
| 32 | 1 | 0.01% |
| **รวม** | **8,915** | **100%** |

**สถิติสำคัญ:**
- ระดับ 10-11 มีเพียง **62 คน (0.70%)**
- พนักงาน **98.23%** มีระดับ 10/11 ใน Chain of Command
- ความยาว Chain เฉลี่ย **5.72 ขั้น**

### 7.2 Top 10 กอง/เขต ที่มีพนักงานมากที่สุด

| อันดับ | PARENTORG | จำนวน (คน) |
|--------|-----------|------------|
| 1 | การประปาส่วนภูมิภาค สาขาส | 575 |
| 2 | การประปาส่วนภูมิภาค สาขาเ | 538 |
| 3 | การประปาส่วนภูมิภาค สาขาพ | 446 |
| 4 | การประปาส่วนภูมิภาค สาขาบ | 418 |
| 5 | กองระบบผลิตและควบคุมคุณภา | 283 |
| 6 | การประปาส่วนภูมิภาค สาขาก | 282 |
| 7 | การประปาส่วนภูมิภาค สาขาน | 281 |
| 8 | กองบริหารทั่วไป | 277 |
| 9 | กองระบบจำหน่าย | 272 |
| 10 | กองแผนและวิชาการ | 270 |

### 7.3 Top 10 ผู้จัดการที่มีลูกน้องมากที่สุด

| PERNR | ชื่อ | ตำแหน่ง | PERSK | จำนวนลูกน้อง |
|-------|------|---------|-------|--------------|
| 14573 | นาย ชยพล ศรีหงอก | หัวหน้างานผลิต 8 | 8 | 43 |
| 5962 | นาย สิงหชัย อินทพิชัย | ผอ.การประปาส่วนภูมิภาคเขต 4 | 11 | 40 |
| 8899 | นาย เอกรินทร์ ภู่พงษ์ | ผอ.การประปาส่วนภูมิภาคเขต 2 | 11 | 36 |
| 13030 | นาย วิสูต ขันธ์เขต | หัวหน้างานผลิต 8 | 8 | 36 |
| 7268 | นาย ธนกร ศิลปะรายะ | ผช.ผู้ว่าการ (บริหารองค์กร) | 10 | 34 |
| 9438 | นาย พงษ์ศักดิ์ เดี่ยววิไล | ผอ.การประปาส่วนภูมิภาคเขต 9 | 11 | 33 |
| 12270 | นาย ธนาทิต ไตรประวัติ | หัวหน้างานผลิต 8 | 8 | 32 |
| 9948 | นาย สาณ เรืองขจร | ผอ.การประปาส่วนภูมิภาคเขต 3 | 11 | 29 |
| 6104 | นาย สมพร นันตะ | ผอ.การประปาส่วนภูมิภาคเขต 6 | 11 | 29 |
| 14533 | ว่าที่ ร.ต. กิตติพงศ์ ศรีพราย | หัวหน้างานผลิต 8 | 8 | 29 |

---

## 8. Data Structure แนะนำ

### 8.1 Database Schema

#### Table: employees
```sql
CREATE TABLE employees (
    PERNR INT PRIMARY KEY,
    ENAME VARCHAR(255) NOT NULL,
    STELL VARCHAR(255),
    PERSK INT,
    ORGEH VARCHAR(255),
    ORGEHID INT,
    PARENTORG VARCHAR(255),  -- สำคัญ! สำหรับ Visibility
    PARENTORGID INT,
    MGRPERNR INT,            -- Foreign Key to employees(PERNR)
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    
    INDEX idx_mgrpernr (MGRPERNR),
    INDEX idx_parentorg (PARENTORG),
    INDEX idx_persk (PERSK)
);
```

#### Table: work_results
```sql
CREATE TABLE work_results (
    id INT PRIMARY KEY AUTO_INCREMENT,
    employee_pernr INT NOT NULL,
    title VARCHAR(255) NOT NULL,
    description TEXT,
    status ENUM('PENDING', 'IN_PROGRESS', 'APPROVED', 'REJECTED', 'SENT_TO_COMMITTEE') DEFAULT 'PENDING',
    current_evaluator_pernr INT,  -- ผู้ประเมินคนปัจจุบัน
    evaluation_count INT DEFAULT 0,  -- จำนวนครั้งที่ถูกประเมิน
    committee_stage ENUM('NONE', 'PRE_FINAL', 'FINAL') DEFAULT 'NONE',
    submitted_at TIMESTAMP,
    completed_at TIMESTAMP,
    
    FOREIGN KEY (employee_pernr) REFERENCES employees(PERNR),
    FOREIGN KEY (current_evaluator_pernr) REFERENCES employees(PERNR),
    INDEX idx_status (status),
    INDEX idx_evaluator (current_evaluator_pernr)
);
```

#### Table: evaluation_history
```sql
CREATE TABLE evaluation_history (
    id INT PRIMARY KEY AUTO_INCREMENT,
    work_result_id INT NOT NULL,
    evaluator_pernr INT NOT NULL,
    evaluator_level INT,  -- PERSK ของผู้ประเมิน
    evaluation_order INT,  -- ลำดับที่ในการประเมิน (1, 2, 3, ...)
    status ENUM('APPROVED', 'REJECTED', 'PENDING') DEFAULT 'PENDING',
    comments TEXT,
    evaluated_at TIMESTAMP,
    
    FOREIGN KEY (work_result_id) REFERENCES work_results(id) ON DELETE CASCADE,
    FOREIGN KEY (evaluator_pernr) REFERENCES employees(PERNR),
    INDEX idx_work_result (work_result_id),
    INDEX idx_evaluator (evaluator_pernr)
);
```

#### Table: committee_members
```sql
CREATE TABLE committee_members (
    id INT PRIMARY KEY AUTO_INCREMENT,
    employee_pernr INT NOT NULL,
    committee_stage ENUM('PRE_FINAL', 'FINAL') NOT NULL,
    parentorg_filter VARCHAR(255),  -- NULL = เห็นทั้งหมด, มีค่า = เห็นเฉพาะ PARENTORG นี้
    is_active BOOLEAN DEFAULT TRUE,
    assigned_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    
    FOREIGN KEY (employee_pernr) REFERENCES employees(PERNR),
    UNIQUE KEY unique_member (employee_pernr, committee_stage),
    INDEX idx_stage (committee_stage),
    INDEX idx_parentorg (parentorg_filter)
);
```

### 8.2 API Endpoints แนะนำ

#### 1. ส่งผลงาน
```typescript
POST /api/work-results
Body: {
  employee_pernr: number,
  title: string,
  description: string,
  attachments?: File[]
}
Response: {
  id: number,
  status: string,
  next_evaluator: {
    pernr: number,
    name: string,
    position: string
  }
}
```

#### 2. ดึงรายการผลงานที่ต้องประเมิน (มี Visibility Filter)
```typescript
GET /api/evaluations/pending?evaluator_pernr={pernr}
Response: {
  total: number,
  items: [
    {
      id: number,
      employee: {...},
      title: string,
      submitted_at: string,
      evaluation_order: number,
      evaluation_count: number
    }
  ]
}

// Backend Logic:
// 1. ดึงข้อมูลผู้ประเมิน (evaluator)
// 2. ตรวจสอบว่าเป็นคณะกรรมการหรือไม่
// 3. Filter ตาม PARENTORG
// SELECT * FROM work_results wr
// INNER JOIN employees e ON wr.employee_pernr = e.PERNR
// WHERE wr.current_evaluator_pernr = {pernr}
// AND (
//   -- ถ้าเป็นคณะกรรมการลำดับสุดท้าย เห็นทั้งหมด
//   EXISTS (SELECT 1 FROM committee_members WHERE employee_pernr = {pernr} AND committee_stage = 'FINAL')
//   OR
//   -- ถ้าเป็นคณะกรรมการกลั่นกรอง เห็นเฉพาะ PARENTORG ของตัวเอง
//   e.PARENTORG = (SELECT PARENTORG FROM employees WHERE PERNR = {pernr})
// )
```

#### 3. ประเมินผลงาน
```typescript
POST /api/evaluations/{work_result_id}/evaluate
Body: {
  evaluator_pernr: number,
  status: "APPROVED" | "REJECTED",
  comments?: string
}
Response: {
  success: boolean,
  next_action: "SENT_TO_NEXT" | "SENT_TO_COMMITTEE" | "COMPLETED",
  next_evaluator?: {...}
}
```

#### 4. ติดตาม Chain of Command
```typescript
GET /api/work-results/{id}/chain
Response: {
  work_result_id: number,
  chain: [
    {
      order: number,
      evaluator: {...},
      status: string,
      evaluated_at: string | null
    }
  ],
  current_step: number,
  total_steps: number
}
```

### 8.3 Frontend State Management แนะนำ

```typescript
interface WorkResult {
  id: number;
  employee: Employee;
  title: string;
  description: string;
  status: EvaluationStatus;
  currentEvaluator?: Employee;
  evaluationCount: number;
  committeeStage: CommitteeStage;
  chain: EvaluationStep[];
  submittedAt: Date;
}

interface EvaluationStep {
  order: number;
  evaluator: Employee;
  evaluatorLevel: number;
  status: "PENDING" | "APPROVED" | "REJECTED";
  evaluatedAt?: Date;
  comments?: string;
}

interface Employee {
  PERNR: number;
  ENAME: string;
  STELL: string;
  PERSK: number;
  ORGEH: string;
  PARENTORG: string;
  MGRPERNR?: number;
}
```

---

## 9. Checklist สำหรับ Dev

### 9.1 Backend Development

#### Phase 1: Database Setup
- [ ] สร้าง Tables ตาม Schema ที่แนะนำ
- [ ] Import ข้อมูลพนักงานจาก Excel (8,915 คน)
- [ ] Validate ความสมบูรณ์ของข้อมูล (MGRPERNR, PARENTORG)
- [ ] สร้าง Indexes สำหรับ Performance
- [ ] Setup Database Backup

#### Phase 2: Core Logic Implementation
- [ ] **Function: getChainOfCommand(pernr)** - สร้าง Chain ตามสายบังคับบัญชา
- [ ] **Function: sendToNextEvaluator(workResult)** - ส่งต่อไปผู้ประเมินถัดไป
  - [ ] ตรวจสอบ PARENTORG (NULL → ส่งเข้าคณะกรรมการ)
  - [ ] ตรวจสอบ MGRPERNR (NULL → ส่งเข้าคณะกรรมการ)
  - [ ] ตรวจสอบ PERSK ของ manager ถัดไป
  - [ ] เงื่อนไข: evaluationCount >= 2 + PERSK >= 10
  - [ ] เงื่อนไข: evaluationCount < 2 + PERSK >= 10 (บังคับประเมิน)
  - [ ] ป้องกัน Circular Reference
- [ ] **Function: checkVisibility(evaluatorPernr, workResultPernr)** - ตรวจสอบว่าผู้ประเมินเห็นผลงานหรือไม่
  - [ ] ถ้าเป็นคณะกรรมการลำดับสุดท้าย → เห็นทั้งหมด
  - [ ] ถ้าเป็นคณะกรรมการกลั่นกรอง → เห็นเฉพาะ PARENTORG ของตัวเอง
  - [ ] ถ้าเป็นพนักงานทั่วไป → เห็นเฉพาะ PARENTORG ของตัวเอง

#### Phase 3: API Development
- [ ] POST /api/work-results - ส่งผลงาน
- [ ] GET /api/evaluations/pending - ดึงรายการผลงานที่ต้องประเมิน (with Visibility)
- [ ] POST /api/evaluations/{id}/evaluate - ประเมินผลงาน
- [ ] GET /api/work-results/{id}/chain - ติดตาม Chain
- [ ] GET /api/employees/{pernr}/chain - ดู Chain ของพนักงาน
- [ ] API Authentication & Authorization
- [ ] Rate Limiting

#### Phase 4: Committee Management
- [ ] สร้างระบบจัดการคณะกรรมการ
  - [ ] เพิ่ม/ลบ สมาชิกคณะกรรมการ
  - [ ] กำหนด PARENTORG Filter สำหรับคณะกรรมการกลั่นกรอง
- [ ] Notification System
  - [ ] แจ้งเตือนผู้ประเมินเมื่อมีผลงานใหม่
  - [ ] แจ้งเตือนผู้ส่งผลงานเมื่อได้รับการประเมิน

#### Phase 5: Testing
- [ ] Unit Tests สำหรับ Core Logic
  - [ ] Test Case 1: Chain สั้น (ไม่มีระดับ 10)
  - [ ] Test Case 2: Chain ยาว (มีระดับ 10, evaluationCount >= 2)
  - [ ] Test Case 3: บังคับประเมินที่ 10/11
  - [ ] Test Case 4: ไม่มี PARENTORG
  - [ ] Test Case 5: Circular Reference
  - [ ] Test Edge Cases ทั้งหมด
- [ ] Integration Tests
- [ ] Load Testing (8,915 users)

### 9.2 Frontend Development

#### Phase 1: UI Components
- [ ] หน้าส่งผลงาน
  - [ ] Form Input (Title, Description, Attachments)
  - [ ] แสดง Preview Chain of Command ก่อนส่ง
- [ ] หน้ารายการผลงานที่ต้องประเมิน
  - [ ] Filter ตาม PARENTORG (Auto)
  - [ ] Search/Sort
  - [ ] Pagination
- [ ] หน้าประเมินผลงาน
  - [ ] แสดงรายละเอียดผลงาน
  - [ ] แสดง Chain History
  - [ ] Form ประเมิน (Approve/Reject + Comments)
- [ ] หน้า Dashboard
  - [ ] สถิติผลงานที่ส่ง/รอประเมิน/เสร็จสิ้น
  - [ ] แสดง Pending Tasks

#### Phase 2: Features
- [ ] Real-time Notifications
- [ ] Export Reports (PDF/Excel)
- [ ] Mobile Responsive
- [ ] Dark Mode

#### Phase 3: Testing
- [ ] UI/UX Testing
- [ ] Cross-browser Testing
- [ ] Mobile Testing
- [ ] Accessibility Testing

### 9.3 Deployment

- [ ] Environment Setup (Dev/Staging/Production)
- [ ] CI/CD Pipeline
- [ ] Database Migration Scripts
- [ ] Monitoring & Logging
- [ ] Backup Strategy
- [ ] Documentation (API Docs, User Manual)

---

## 10. สรุป

### 10.1 Key Takeaways

1. **Flow การประเมิน** มี 3 ขั้นตอนหลัก:
   - ประเมินตามสายบังคับบัญชา (สูงสุดถึงระดับ 10)
   - คณะกรรมการกลั่นกรอง (เห็นเฉพาะ PARENTORG ของตัวเอง)
   - คณะกรรมการลำดับสุดท้าย (เห็นทั้งหมด)

2. **เงื่อนไขสำคัญ:**
   - ส่งต่อได้สูงสุดถึงระดับ 10 (PERSK = 10)
   - มีการประเมิน >= 2 ครั้ง + ลำดับถัดไปคือ 10/11 → ข้ามไป "ก่อนลำดับสุดท้าย"
   - ยังไม่มีการประเมิน + ลำดับถัดไปคือ 10/11 → บังคับประเมินก่อน

3. **Visibility Rules:**
   - พนักงานทั่วไป: เห็นเฉพาะ PARENTORG ของตัวเอง
   - คณะกรรมการกลั่นกรอง: เห็นเฉพาะ PARENTORG ของตัวเอง
   - คณะกรรมการลำดับสุดท้าย: เห็นทั้งหมด

4. **Edge Cases ที่ต้องจัดการ:**
   - ไม่มี PARENTORG (5 คน)
   - ไม่มี MGRPERNR (4 คน)
   - Circular Reference
   - MGRPERNR ไม่มีในระบบ
   - ระดับพิเศษ (21, 31, 32)

### 10.2 ข้อแนะนำสำหรับการพัฒนา

1. **เริ่มจาก Core Logic ก่อน:** 
   - สร้าง Function `getChainOfCommand()` และ `sendToNextEvaluator()` ให้ถูกต้องก่อน
   - Test Case ต่างๆ ให้ครบถ้วน

2. **จัดการ Visibility อย่างระมัดระวัง:**
   - ใช้ Database Query ที่ Optimize แล้ว
   - Cache ข้อมูล PARENTORG ของผู้ใช้

3. **Monitoring & Logging:**
   - Log ทุก Step ของ Flow การประเมิน
   - Alert เมื่อเจอ Edge Cases

4. **Performance:**
   - Index ที่สำคัญ: MGRPERNR, PARENTORG, PERSK
   - Cache Chain of Command ที่คำนวณแล้ว

---

**หมายเหตุ:** เอกสารนี้จัดทำขึ้นจากการวิเคราะห์ข้อมูล 8,915 คน ใน 166 กอง/เขต ณ วันที่ 23 พฤศจิกายน 2025

**ติดต่อสอบถามเพิ่มเติม:**
- Project Coordinator: [ชื่อผู้ประสานงาน]
- Technical Lead: [ชื่อ Tech Lead]

---

**Revision History:**
- v1.0 (23 Nov 2025): เอกสารฉบับแรก
